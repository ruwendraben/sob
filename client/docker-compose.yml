services:
  app:
    build: .
    container_name: public-image-timeline
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - timeline_data:/app/data
    restart: unless-stopped

volumes:
  timeline_data:
    name: timeline_data
